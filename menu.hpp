#include <iostream>
#define MENU_SIZE 6

/**
 * Obsługa menu. Pobiera od użytkownika wybraną opcję.
 * @param input Wskaźnik na wybraną opcję.
*/
void menu(short *input);

/**
 * Pobiera promień od urzytkownika i przypisuje do zmiennej.
 * @param r Wskaźnik na promień.
*/
void getValue(float *r);